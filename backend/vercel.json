{
  "version": 2,
  "builds": [
    {
      "src": "app.py",
      "use": "@vercel/python@3.12.0",
      "config": {
        "maxDuration": 60,
        "memory": 2048
      }
    }
  ],
  "installCommand": "pip install --break-system-packages -r requirements.txt",
  "routes": [
    { "src": "/(.*)", "dest": "app.py" }
  ]
}