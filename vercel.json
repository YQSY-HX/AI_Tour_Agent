{
  "version": 2,
  "builds": [
    {
      "src": "backend/app.py",
      "use": "@vercel/python@3.12.0",
      "config": {
        "maxDuration": 60,
        "memory": 2048
      }
    }
  ],
  "installCommand": "pip install --break-system-packages -r backend/requirements.txt",
  "routes": [
    { "src": "/(.*)", "dest": "backend/app.py" }
  ]
}